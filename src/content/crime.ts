import { emptyPack } from '@/engine/registry';
import type { ContentPack } from '@/types';

/** Committable crimes with success odds, payouts and sentences, plus prison events. */
export const crimePack: ContentPack = emptyPack('crime');
