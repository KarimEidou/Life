import { emptyPack } from '@/engine/registry';
import type { ContentPack } from '@/types';

/** Playable countries with their cost, tax and visa-difficulty multipliers. */
export const countriesPack: ContentPack = emptyPack('countries');
