import { emptyPack } from '@/engine/registry';
import type { ContentPack } from '@/types';

/** Fame-gated content: social posts, interviews, scandals and endorsement deals. */
export const famePack: ContentPack = emptyPack('fame');
