import { emptyPack } from '@/engine/registry';
import type { ContentPack } from '@/types';

/** Per-country given-name and surname pools used to generate characters and family. */
export const namesPack: ContentPack = emptyPack('names');
