import { emptyPack } from '@/engine/registry';
import type { ContentPack } from '@/types';

/** Degree-gated career tracks: medicine, law, tech, finance, academia and fame. */
export const jobsProfessionalPack: ContentPack = emptyPack('jobs-professional');
