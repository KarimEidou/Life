import { emptyPack } from '@/engine/registry';
import type { ContentPack } from '@/types';

/** Entry-level, part-time and trade career tracks that need no degree. */
export const jobsOrdinaryPack: ContentPack = emptyPack('jobs-ordinary');
