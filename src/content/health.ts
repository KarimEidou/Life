import { emptyPack } from '@/engine/registry';
import type { ContentPack } from '@/types';

/** Illness definitions, treatments and the doctor/hospital interactions. */
export const healthPack: ContentPack = emptyPack('health');
