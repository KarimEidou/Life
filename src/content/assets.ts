import { emptyPack } from '@/engine/registry';
import type { ContentPack } from '@/types';

/** Buyable properties and vehicles with their upkeep and appreciation rates. */
export const assetsPack: ContentPack = emptyPack('assets');
