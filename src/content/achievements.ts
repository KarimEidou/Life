import { emptyPack } from '@/engine/registry';
import type { ContentPack } from '@/types';

/** Cross-life achievements and the state checks that unlock them. */
export const achievementsPack: ContentPack = emptyPack('achievements');
