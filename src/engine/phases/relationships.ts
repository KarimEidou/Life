/**
 * Relationships phase: everyone else ages, drifts and occasionally leaves.
 */

import type { Ctx, LogEntry } from '@/types';

/** Ages every person, drifts affinity, rolls new meetings, breakups and deaths. */
export function relationshipsPhase(ctx: Ctx): LogEntry[] {
  throw new Error('TODO:relationships.relationshipsPhase');
}
