import type { CSSProperties, ReactElement } from 'react';

const placeholderStyle: CSSProperties = {
  display: 'flex',
  flex: 1,
  minHeight: 0,
  alignItems: 'center',
  justifyContent: 'center',
  padding: 'var(--sp-5)',
  color: 'var(--label-2)',
  fontSize: 'var(--fs-subhead)',
};

/** The overflow menu: achievements, emigration, settings and the rest. */
export function MoreSheet(): ReactElement {
  return <div style={placeholderStyle}>MoreSheet</div>;
}
